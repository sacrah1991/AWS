Amazon ec2 stands for Elastic Compute Cloude. Ec2 provides scalable computing capacity in the aws cloud 
using EC2 eliminates your need to invest in hardware upfront so you can devlop and deploy application.
You can use AWS Ec2 to launch one or more virtual servers as you need. configure security and networking and 
manages storage.

bydefault when you create an EC2 account with aws your account is limited to a max.20 instance
